import { OriginaldataPipe } from './originaldata.pipe';

describe('OriginaldataPipe', () => {
  it('create an instance', () => {
    const pipe = new OriginaldataPipe();
    expect(pipe).toBeTruthy();
  });
});
